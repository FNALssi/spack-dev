#!/usr/bin/env python

from __future__ import print_function

class External_package:
    def __init__(self, name, version, pathname):
        self.name = name
        self.version = version
        self.pathname = pathname
